package org.generation.syntaxlogistics2.enums;

public enum ServiceType {
    SHARED,        // Compartido (Desde $80)
    EXPRESS,       // Express (Desde $120)
    EXCLUSIVE,     // Exclusivo (Desde $340)
    EXTRAORDINARY  // Extraordinario (Cotización personalizada)
}